# Slag for Slack

Generated by `package.mjs` — edit the palette, not this file.

Slack only exposes four colors, so this is a hand-entered theme rather than
something you install. In Slack: **Preferences → Themes → Custom theme**, set
the color mode to match, then paste each value below.

Leave **Window gradient** off. It blends the background toward the accent,
which breaks the one-background-gray rule the rest of the theme runs on.

## Slag

Set the color mode to **Dark**.

| Slack setting | Color | What it is |
| ------------- | ----- | ---------- |
| System navigation | `#282828` | the sidebar, which is the one background gray |
| Selected items | `#fabd2f` | the active channel, matching the editor's accent |
| Presence indication | `#b8bb26` | the online dot, same green as a git addition |
| Notifications | `#fb4934` | the unread badge, same red as a deletion |

## Slag Light

Set the color mode to **Light**.

| Slack setting | Color | What it is |
| ------------- | ----- | ---------- |
| System navigation | `#f0f0f0` | the sidebar, which is the one background gray |
| Selected items | `#b57614` | the active channel, matching the editor's accent |
| Presence indication | `#6b670d` | the online dot, same green as a git addition |
| Notifications | `#9d0006` | the unread badge, same red as a deletion |
