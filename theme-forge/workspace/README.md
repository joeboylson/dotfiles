# Sample Workspace

This workspace exists so the screenshots have something real to show.

## Why

A theme looks different against actual code than against an empty editor.
Keep a few file types here: TypeScript, JSON, Markdown.

- Lists render here
- `inline code` renders here
- [links](https://example.com) render here

> Block quotes too.
